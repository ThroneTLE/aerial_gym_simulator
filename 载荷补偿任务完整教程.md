# 载荷补偿任务完整教程

本教程详细介绍如何使用载荷补偿任务进行训练、测试和部署。

---

## 目录

1. [项目概述](#项目概述)
2. [环境配置](#环境配置)
3. [任务说明](#任务说明)
4. [训练流程](#训练流程)
5. [测试与可视化](#测试与可视化)
6. [配置参数详解](#配置参数详解)
7. [常见问题](#常见问题)
8. [高级用法](#高级用法)

---

## 项目概述

### 技术背景

本项目实现了基于**潜变量蒸馏**的抗扰动控制系统，用于解决子母无人机释放载荷时的姿态扰动问题。

**核心思想**：
- **8 维潜变量 $z_t$**：表征系统的物理特性（惯性、电机特性、载荷等不可直接观测的参数）
- **主策略网络（MLP）**：输入是 **基础观测 + 8维潜变量**，输出补偿动作
- **第一阶段（教师训练）**：使用特权编码器从 41 维特权观测中编码得到 8 维潜变量
- **第二阶段（学生蒸馏）**：使用 1D-CNN 从历史传感器数据推断出这 8 维潜变量（尚未实现）

### 系统架构

#### 第一阶段：教师网络训练

```
┌─────────────────────────────────────────────────────────────────┐
│                    第一阶段：教师网络训练                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────┐                                            │
│  │   特权观测 41维   │                                            │
│  │ (privileged_obs) │                                            │
│  │ 载荷质量/质心/惯量等 │                                          │
│  └────────┬─────────┘                                            │
│           │                                                       │
│           v                                                       │
│  ┌──────────────────┐                                            │
│  │   特权编码器      │                                            │
│  │ (priv_encoder)   │                                            │
│  │ 41 → 128 → 128 → 8│                                           │
│  └────────┬─────────┘                                            │
│           │                                                       │
│           │ 8维潜变量 z_t                                         │
│           │ (表征物理特性)                                         │
│           │                                                       │
│           │              ┌──────────────────┐                    │
│           │              │   基础观测 29维   │                    │
│           │              │     (obs)        │                    │
│           │              │ 位置误差/姿态/速度等│                   │
│           │              └────────┬─────────┘                    │
│           │                       │                               │
│           └───────────┬───────────┘                               │
│                       │ 拼接                                      │
│                       v                                           │
│              ┌─────────────────┐                                  │
│              │ 主策略网络 (MLP)│                                   │
│              │ 输入: 29 + 8 = 37维│                               │
│              │ 256 → 256 → 128  │                                 │
│              └────────┬────────┘                                  │
│                       │                                           │
│                       v                                           │
│              ┌─────────────────┐                                  │
│              │ 补偿动作 4维     │                                  │
│              │ [推力,τx,τy,τz]  │                                  │
│              └─────────────────┘                                  │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

#### 第二阶段：学生网络蒸馏（待实现）

```
┌─────────────────────────────────────────────────────────────────┐
│                    第二阶段：学生网络蒸馏（待实现）                │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────┐                                    │
│  │  历史传感器数据 (100步)   │                                    │
│  │  形状: (100, obs_dim)    │                                    │
│  └────────────┬─────────────┘                                    │
│               │                                                   │
│               v                                                   │
│  ┌──────────────────────────┐                                    │
│  │      1D-CNN 编码器        │       ┌─────────────────────┐     │
│  │  从历史数据推断潜变量      │       │   教师编码器输出     │     │
│  │  → 8维潜变量 ẑ_t          │ <===> │   8维潜变量 z_t     │     │
│  └────────────┬─────────────┘       │   (监督学习目标)    │     │
│               │                      └─────────────────────┘     │
│               │ 8维推断的潜变量                                   │
│               │                                                   │
│               │              ┌──────────────────┐                │
│               │              │   基础观测 29维   │                │
│               │              └────────┬─────────┘                │
│               │                       │                           │
│               └───────────┬───────────┘                           │
│                           │ 拼接                                  │
│                           v                                       │
│                  ┌─────────────────┐                              │
│                  │ 主策略网络 (MLP)│  <== 冻结（复用第一阶段训练的）│
│                  │  （同第一阶段）   │                              │
│                  └────────┬────────┘                              │
│                           │                                       │
│                           v                                       │
│                  ┌─────────────────┐                              │
│                  │ 补偿动作 4维     │                              │
│                  └─────────────────┘                              │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

> [!IMPORTANT]
> **当前代码状态**：第一阶段已完整实现，第二阶段的 1D-CNN 学生网络**尚未实现**。

---

## 环境配置

### 1. 系统要求

- **操作系统**：Ubuntu 18.04/20.04
- **Python**：3.8
- **CUDA**：11.5+
- **GPU**：NVIDIA GPU（推荐 RTX 3060 及以上）

### 2. 安装依赖

```bash
# 激活 conda 环境
conda activate aerialgym

# 验证环境
python -c "import torch; print(f'PyTorch: {torch.__version__}')"
python -c "from isaacgym import gymapi; print('Isaac Gym: OK')"
```

### 3. 项目结构

```
aerial_gym_simulator/
├── aerial_gym/
│   ├── task/
│   │   └── payload_compensation_task/
│   │       ├── payload_compensation_task.py    # 主任务类
│   │       ├── payload_manager.py              # 载荷管理
│   │       ├── reward_calculator.py            # 奖励计算
│   │       └── teacher_residual.py             # 教师残差
│   ├── config/
│   │   └── task_config/
│   │       ├── payload_compensation_task_config.py         # 基础配置
│   │       └── payload_compensation_task_teacher_config.py # 教师配置
│   ├── examples/
│   │   └── play_policy.py                      # 统一推理脚本
│   └── rl_training/
│       └── rl_games/
│           ├── runner.py                       # 训练入口
│           └── ppo_aerial_quad.yaml            # PPO 配置
└── docs/
    └── 载荷补偿任务完整教程.md                  # 本教程
```

---

## 任务说明

### 观测空间

#### 基础观测（29 维）
学生网络可见的观测：

| 维度 | 内容 | 说明 |
|------|------|------|
| 0-2 | 位置误差 | 目标位置 - 当前位置 (x, y, z) |
| 3-11 | 旋转矩阵 | 机体姿态的旋转矩阵（展平为 9 维）|
| 12-14 | 线速度 | 机体坐标系下的线速度 (vx, vy, vz) |
| 15-17 | 角速度 | 机体坐标系下的角速度 (ωx, ωy, ωz) |
| 18-20 | 载荷质量 | 当前附着载荷的总质量（归一化）|
| 21-23 | 质心偏移 | 载荷引起的质心偏移 (x, y, z) |
| 24-26 | 上次释放质量 | 上一次释放的载荷质量（归一化）|
| 27 | 附着标志 | 是否有载荷附着 (0/1) |
| 28 | 预警标志 | 是否即将释放载荷 (0/1) |

#### 特权观测（41 维）
仅教师网络可见，包含完整的物理参数：
- 基础观测（29 维）
- 精确的载荷质量、惯量矩阵
- 真实的质心偏移
- 其他物理参数

### 动作空间（4 维）

| 维度 | 内容 | 范围 | 说明 |
|------|------|------|------|
| 0 | 推力补偿 | [-1, 1] | 额外的垂直推力，缩放到 ±2.0 N |
| 1 | Roll 力矩补偿 | [-1, 1] | 绕 X 轴的补偿力矩，缩放到 ±0.5 Nm |
| 2 | Pitch 力矩补偿 | [-1, 1] | 绕 Y 轴的补偿力矩，缩放到 ±0.5 Nm |
| 3 | Yaw 力矩补偿 | [-1, 1] | 绕 Z 轴的补偿力矩，缩放到 ±0.1 Nm |

**注意**：这些补偿动作会叠加到 Lee 位置控制器的输出上。

### 奖励函数

#### 教师模式（Teacher Mode）
主要依赖存活奖励和模仿学习：

```python
reward = survive_bonus + imitation_reward + crash_penalty
```

- **存活奖励**：每步 +10.0（鼓励长时间飞行）
- **模仿奖励**：-8.0 × ||a_student - a_teacher||²（鼓励模仿教师）
- **崩溃惩罚**：-10.0（碰撞或倾斜过大）

#### 基础模式（Base Mode）
使用位置跟踪和稳定性奖励：

```python
reward = position_reward - penalties
```

详见配置文件中的 `reward_parameters`。

---

## 训练流程

### 第一阶段：教师策略训练

教师网络使用特权信息学习最优补偿策略。

#### 1. 配置检查

查看教师配置文件：
```bash
cat aerial_gym/config/task_config/payload_compensation_task_teacher_config.py
```

关键参数：
- `teacher_mode = True`：启用教师模式
- `observation_space_dim = 29`：基础观测维度
- `privileged_observation_space_dim = 41`：特权观测维度
- `imitation_weight = 8.0`：模仿学习权重

#### 2. 启动训练

```bash
cd /home/throne/workspaces/aerial_gym_ws/src/aerial_gym_simulator

# 使用默认配置训练
python aerial_gym/rl_training/rl_games/runner.py \
    --task payload_compensation_task_teacher \
    --num_envs 2048 \
    --headless

# 自定义参数训练
python aerial_gym/rl_training/rl_games/runner.py \
    --task payload_compensation_task_teacher \
    --num_envs 4096 \
    --headless \
    --max_iterations 10000 \
    --save_interval 500
```

**参数说明**：
- `--task`：任务名称（必须与配置文件对应）
- `--num_envs`：并行环境数量（越多越快，但需要更多显存）
- `--headless`：无头模式（不显示图形界面）
- `--max_iterations`：最大训练迭代次数
- `--save_interval`：保存检查点的间隔

#### 3. 监控训练

训练过程中会自动保存日志到 TensorBoard：

```bash
# 在新终端中启动 TensorBoard
tensorboard --logdir=runs --port=6006

# 在浏览器中打开
# http://localhost:6006
```

**关键指标**：
- `rewards/mean`：平均奖励（应逐渐上升）
- `rewards/imitation`：模仿误差（应逐渐下降）
- `episode_lengths/mean`：平均 episode 长度（应接近 1500）
- `losses/policy_loss`：策略损失
- `losses/value_loss`：价值损失

#### 4. 检查点保存

检查点自动保存在：
```
runs/payload_compensation_task_teacher/nn/
├── last_payload_compensation_task_teacher_ep_XXXX_rew_YYY.pth
└── payload_compensation_task_teacher.pth  # 最新检查点的软链接
```

### 第二阶段：学生策略蒸馏（可选）

如果需要纯粹的学生策略（不依赖特权信息），可以进行第二阶段训练。

**注意**：当前配置已经在第一阶段同时训练了学生网络，通常不需要第二阶段。

---

## 测试与可视化

### 使用统一推理脚本

我们提供了 `play_policy.py` 统一推理脚本，支持多种模式。

#### 1. 基本用法

```bash
# 使用 RL-Games 模型加载器（推荐）
python aerial_gym/examples/play_policy.py \
    --mode rlgames \
    --checkpoint runs/payload_compensation_task_teacher/nn/payload_compensation_task_teacher.pth \
    --steps 3000 \
    --headless False \
    --show_plot True
```

**参数说明**：
- `--mode`：模型加载模式
  - `rlgames`：使用 RL-Games 的模型构建器（推荐）
  - `manual`：手动构建 MLP 网络
- `--checkpoint`：检查点文件路径
- `--steps`：运行步数
- `--headless`：是否无头模式（False 显示 Isaac Gym 窗口）
- `--show_plot`：是否显示实时绘图
- `--num_envs`：并行环境数量（默认 1）

#### 2. 可视化模式

显示 Isaac Gym 窗口并实时绘图：

```bash
python aerial_gym/examples/play_policy.py \
    --mode rlgames \
    --checkpoint runs/payload_compensation_task_teacher/nn/payload_compensation_task_teacher.pth \
    --steps 5000 \
    --headless False \
    --show_plot True \
    --num_envs 1
```

**绘图内容**：
- 位置误差（XYZ）
- 姿态角（Roll, Pitch, Yaw）
- 补偿动作（推力 + 3 个力矩）
- 载荷质量变化
- 奖励曲线

#### 3. 批量测试

测试多个环境的性能：

```bash
python aerial_gym/examples/play_policy.py \
    --mode rlgames \
    --checkpoint runs/payload_compensation_task_teacher/nn/payload_compensation_task_teacher.pth \
    --steps 3000 \
    --headless True \
    --num_envs 64
```

#### 4. 调试模式

不加载检查点，使用随机策略测试任务逻辑：

```bash
python aerial_gym/examples/play_policy.py \
    --mode rlgames \
    --no_checkpoint \
    --steps 100 \
    --headless True \
    --show_plot False
```

### 性能评估

训练完成后，评估策略性能：

```bash
# 运行 10 个 episode，记录统计信息
python aerial_gym/examples/play_policy.py \
    --mode rlgames \
    --checkpoint runs/payload_compensation_task_teacher/nn/payload_compensation_task_teacher.pth \
    --steps 15000 \
    --headless True \
    --num_envs 10
```

**评估指标**：
- 平均 episode 长度
- 平均奖励
- 成功率（完成 episode 的比例）
- 位置跟踪误差
- 姿态稳定性

---

## 配置参数详解

### 教师配置文件

路径：`aerial_gym/config/task_config/payload_compensation_task_teacher_config.py`

#### 核心参数

```python
# 教师模式
teacher_mode = True

# 观测空间
observation_space_dim = 29          # 学生观测维度
privileged_observation_space_dim = 41  # 教师观测维度

# 动作空间
action_space_dim = 4                # 补偿动作维度
action_space_limits = [2.0, 0.5, 0.5, 0.1]  # [推力, Roll, Pitch, Yaw]

# Episode 参数
episode_len_steps = 1500            # 每个 episode 的最大步数
```

#### 奖励参数

```python
reward_parameters = {
    # 核心奖励
    "survive_bonus": 10.0,          # 存活奖励（每步）
    "crash_penalty": -10.0,         # 崩溃惩罚
    
    # 模仿学习
    "imitation_weight": 8.0,        # 模仿损失权重
    "imitation_weight_start": 8.0,  # 起始权重
    "imitation_weight_end": 8.0,    # 结束权重
    "imitation_weight_decay_reward": 150.0,  # 衰减阈值
    "imitation_weight_decay_span": 50.0,     # 衰减窗口
    "imitation_reward_ema_alpha": 0.05,      # EMA 平滑系数
}
```

#### 载荷参数

```python
payload_parameters = {
    # 质量参数
    "payload_mass": 0.02,           # 单个载荷质量（kg）
    "payload_mass_range": [0.0, 0.03],  # 随机化范围
    "randomize_payload_mass": True,  # 启用质量随机化
    
    # 质心偏移
    "randomize_offsets_on_plane": True,  # 启用偏移随机化
    "offset_plane_radial_jitter": 0.4,   # 径向扰动
    "offset_plane_z_jitter": 0.8,        # 垂直扰动
    
    # 释放调度
    "release_start": 50,            # 首次释放步数
    "release_interval": 300,        # 释放间隔
    "release_start_range": [50, 100],    # 随机化范围
    "release_interval_range": [300, 350],
    "warning_steps": 100,           # 预警步数
    "randomize_release": True,      # 启用释放随机化
}
```

#### DAgger 参数

```python
# DAgger（Dataset Aggregation）参数
dagger_frac = 0.0                   # 初始教师动作占比
dagger_decay_reward = 15000.0       # 衰减阈值
dagger_decay_rate = 0.99            # 衰减系数
dagger_min_frac = 0.0               # 最小占比
imitation_err_threshold = 0.5       # 误差阈值
```

### PPO 训练配置

路径：`aerial_gym/rl_training/rl_games/ppo_aerial_quad.yaml`

#### 网络结构

```yaml
network:
  name: privileged_actor_critic
  
  # 特权编码器
  privileged_dim: 41        # 特权观测维度
  privileged_embed: 8       # 潜变量维度
  privileged_hidden: 128    # 编码器隐藏层
  
  # 策略网络
  mlp:
    units: [256, 256, 128]  # 隐藏层大小
    activation: elu
    initializer:
      name: default
```

#### PPO 参数

```yaml
params:
  algo:
    name: a2c_continuous
  
  model:
    name: continuous_a2c_logstd
  
  # 训练参数
  config:
    name: payload_compensation_task_teacher
    num_actors: 2048        # 并行环境数
    num_agents: 1
    
    # PPO 超参数
    gamma: 0.99             # 折扣因子
    tau: 0.95               # GAE lambda
    learning_rate: 3e-4     # 学习率
    lr_schedule: adaptive   # 学习率调度
    
    # 训练设置
    mini_epochs: 5          # 每次更新的 epoch 数
    minibatch_size: 16384   # 小批量大小
    horizon_length: 24      # 时间步长
    
    # 裁剪和正则化
    e_clip: 0.2             # PPO 裁剪参数
    entropy_coef: 0.0       # 熵正则化系数
    clip_value: True
    
    # 归一化
    normalize_input: True
    normalize_value: True
```

---

## 常见问题

### Q1: 训练时显存不足怎么办？

**A**: 减少并行环境数量：

```bash
# 从 2048 减少到 1024 或 512
python aerial_gym/rl_training/rl_games/runner.py \
    --task payload_compensation_task_teacher \
    --num_envs 1024 \
    --headless
```

或在配置文件中调整：
```yaml
# ppo_aerial_quad.yaml
config:
  num_actors: 1024  # 减少这个值
```

### Q2: 训练不收敛怎么办？

**A**: 检查以下几点：

1. **查看 TensorBoard**：确认奖励是否在上升
2. **调整学习率**：
   ```yaml
   learning_rate: 1e-4  # 降低学习率
   ```
3. **增加训练时间**：
   ```bash
   --max_iterations 20000  # 增加迭代次数
   ```
4. **检查奖励权重**：确保 `imitation_weight` 不要太大

### Q3: 如何加载特定的检查点？

**A**: 指定完整路径：

```bash
python aerial_gym/examples/play_policy.py \
    --mode rlgames \
    --checkpoint runs/payload_compensation_task_teacher/nn/last_payload_compensation_task_teacher_ep_5000_rew_150.pth \
    --steps 3000
```

### Q4: 如何修改载荷释放策略？

**A**: 编辑配置文件中的 `payload_parameters`：

```python
payload_parameters = {
    "release_start": 100,        # 延迟首次释放
    "release_interval": 500,     # 增加释放间隔
    "randomize_release": False,  # 关闭随机化，使用固定间隔
}
```

### Q5: 如何调整补偿动作的范围？

**A**: 修改 `action_space_limits`：

```python
# 增大推力补偿范围
action_space_limits = [3.0, 0.5, 0.5, 0.1]  # 推力从 ±2.0N 增加到 ±3.0N
```

同时需要修改控制器配置：
```python
# aerial_gym/config/controller_config/lee_controller_with_comp_config.py
compensation_thrust_limit = 1.5  # 对应 action_space_limits[0] / 2
```

### Q6: 训练过程中出现 NaN 怎么办？

**A**: 通常是数值不稳定导致，尝试：

1. **降低学习率**：
   ```yaml
   learning_rate: 1e-4
   ```
2. **启用梯度裁剪**：
   ```yaml
   grad_norm: 1.0
   ```
3. **检查奖励函数**：确保没有除零或 log(0) 的情况

### Q7: 如何在真实无人机上部署？

**A**: 部署流程：

1. **导出策略网络**：
   ```python
   # 提取学生网络（不需要特权编码器）
   import torch
   checkpoint = torch.load('checkpoint.pth')
   student_weights = checkpoint['model']['a2c_network.actor_mlp']
   torch.save(student_weights, 'student_policy.pth')
   ```

2. **转换为 ONNX**（可选）：
   ```python
   import torch.onnx
   dummy_input = torch.randn(1, 29)  # 29 维观测
   torch.onnx.export(model, dummy_input, "policy.onnx")
   ```

3. **集成到飞控**：
   - 实现 29 维观测的采集
   - 加载策略网络
   - 将输出动作发送给控制器

---

## 高级用法

### 自定义奖励函数

编辑 `aerial_gym/task/payload_compensation_task/reward_calculator.py`：

```python
def compute_basic_reward(obs_dict, actions, task_config):
    """自定义奖励函数"""
    
    # 获取状态
    pos_error = obs_dict["position_error"]
    attitude = obs_dict["attitude"]
    
    # 计算自定义奖励
    position_reward = -torch.norm(pos_error, dim=-1)
    attitude_reward = -torch.sum(attitude**2, dim=-1)
    
    # 组合奖励
    total_reward = position_reward + attitude_reward
    
    return total_reward, {}  # 返回总奖励和详细信息字典
```

### 自定义载荷释放策略

编辑 `aerial_gym/task/payload_compensation_task/payload_manager.py`：

```python
class PayloadManager:
    def _schedule_releases(self, env_ids):
        """自定义释放调度"""
        
        # 示例：基于性能的自适应释放
        performance = self.calculate_performance()
        
        for env_id in env_ids:
            if performance[env_id] > threshold:
                # 性能好，提前释放
                self.next_release_step[env_id] = current_step + 200
            else:
                # 性能差，延迟释放
                self.next_release_step[env_id] = current_step + 400
```

### 添加新的观测特征

1. **修改观测维度**：
   ```python
   # payload_compensation_task_teacher_config.py
   observation_space_dim = 32  # 增加 3 维
   ```

2. **修改观测构建**：
   ```python
   # payload_compensation_task.py
   def process_obs_for_task(self):
       # ... 现有观测 ...
       
       # 添加新特征（例如：加速度）
       acceleration = self.compute_acceleration()
       obs_tensor[:, 29:32] = acceleration
       
       return obs_tensor
   ```

3. **更新网络配置**：
   ```yaml
   # ppo_aerial_quad.yaml
   network:
     mlp:
       input_size: 32  # 更新输入维度
   ```

### 课程学习

实现渐进式难度提升：

```python
# payload_compensation_task_teacher_config.py
curriculum_parameters = {
    "enabled": True,
    "stages": [
        {
            "reward_threshold": 50.0,
            "payload_mass_range": [0.0, 0.01],  # 阶段 1：轻载荷
            "release_interval": 500,
        },
        {
            "reward_threshold": 100.0,
            "payload_mass_range": [0.0, 0.02],  # 阶段 2：中等载荷
            "release_interval": 400,
        },
        {
            "reward_threshold": 150.0,
            "payload_mass_range": [0.0, 0.03],  # 阶段 3：重载荷
            "release_interval": 300,
        },
    ]
}
```

### 多任务学习

同时训练多个相关任务：

```bash
# 训练脚本
python aerial_gym/rl_training/rl_games/runner.py \
    --task payload_compensation_task_teacher \
    --multi_task True \
    --task_variants ["light_payload", "heavy_payload", "asymmetric_payload"]
```

---

## 性能优化建议

### 训练加速

1. **使用更多 GPU**：
   ```bash
   CUDA_VISIBLE_DEVICES=0,1 python aerial_gym/rl_training/rl_games/runner.py ...
   ```

2. **增加并行环境**：
   ```bash
   --num_envs 4096  # 如果显存足够
   ```

3. **调整 horizon_length**：
   ```yaml
   horizon_length: 32  # 增加可以提高效率，但可能影响稳定性
   ```

### 推理加速

1. **使用 TorchScript**：
   ```python
   scripted_model = torch.jit.script(model)
   scripted_model.save("policy_jit.pt")
   ```

2. **批量推理**：
   ```bash
   --num_envs 64  # 批量测试多个环境
   ```

---

## 参考资料

- **技术文档**：`技术原理与推导：基于潜变量蒸馏的抗扰动控制.md`
- **变更日志**：`CODEX_CHANGELOG.md`
- **重构记录**：`docs/walkthrough.md`
- **Isaac Gym 文档**：https://developer.nvidia.com/isaac-gym
- **RL-Games 文档**：https://github.com/Denys88/rl_games

---

## 联系与支持

如有问题，请：
1. 查看本教程的常见问题部分
2. 检查 TensorBoard 日志
3. 查看代码注释和文档字符串

**祝训练顺利！** 🚁



> **摘要**：本研究旨在解决四足机器人在复杂动态环境（如未知载荷、外部推力）下的抗扰动控制问题。我们采用“教师-学生”架构：第一阶段构建一个全知的“教师”策略，利用精确的物理模型计算出完美的抗扰动动作；第二阶段训练一个仅依赖机载传感器的“学生”策略（基于 CNN），通过观察历史姿态序列来“模仿”教师的行为。为了缩小仿真与现实的差距，我们引入 PINN 思想对学生网络进行物理一致性约束，使其在未知工况下仍能遵循基本动力学准则。

## 1. 问题背景与数学建模

本研究的核心挑战在于：机器人在真实环境中往往面临许多**不可直接观测**的物理参数变化，例如：

- **地面摩擦系数**的变化。
    
- **外部推力**或风阻的干扰。
    
- **负载质量**及其分布的改变。
    

我们将这一问题建模为**部分可观测马尔可夫决策过程 (POMDP)**。

- **状态空间 (**$S$**)**：包含所有物理状态，包括那些机器人看不见的参数。
    
- **动作空间 (**$A$**)**：机器人的控制指令。
    
- **特权信息 (**$e_t$**)**：我们将那些机器人看不见但对控制至关重要的信息统称为“特权信息”。
    

## 2. 第一阶段：全知教师策略 (The Omniscient Teacher)

在仿真环境中，我们拥有“上帝视角”，可以直接获取所有特权信息。利用这一优势，我们构建了一个**解析教师 (Analytical Teacher)**。

### 2.1 教师“看”到了什么？（特权信息向量）

教师的输入是一个精确描述环境动力学的特征向量 $e_t \in \mathbb{R}^{41}$，它由以下五部分拼接而成：

$$e_t = [ \mathbf{p}_{load}, \mathbf{p}_{inertia}, \mathbf{p}_{hist}, \mathbf{p}_{ctrl}, \mathbf{p}_{dist} ]$$

1. **载荷特性** ($\mathbf{p}_{load}$)：到底挂了多重的东西？质心偏了多少？
    
2. **惯性特性** ($\mathbf{p}_{inertia}$)：机体转动惯量是多少？
    
3. **历史动力学** ($\mathbf{p}_{hist}$)：上一时刻载荷产生了多大的干扰力矩？
    
4. **控制分配参数** ($\mathbf{p}_{ctrl}$)：电机推力如何映射到机体受力？
    
5. **扰动参数** ($\mathbf{p}_{dist}$)：环境目前施加的最大干扰力和力矩上限。
    

### 2.2 教师如何处理信息？（潜在空间编码）

直接处理 41 维的原始数据容易导致“维数灾难”。因此，我们设计了一个基于 **多层感知机 (MLP)** 的编码器 $\mathcal{E}$，将这些繁杂的信息压缩成一个精简的 **8维编码 (**$z_t$**)**。

$$z_t = \mathcal{E}_{MLP}(e_t)$$

这个 $z_t$ 就像是环境的“指纹”。 **注意**：在这一步，如果仅通过端到端训练，$z_t$ 只是一个隐式的数字特征，**并不一定具有明确的物理意义**。要保证 $z_t$ 真的代表“质量”或“摩擦”，需要后续的辅助任务约束（见 6.1 节）。

### 2.3 教师如何控制？（物理残差策略）

我们的教师并不是从零开始瞎猜，而是基于**物理模型**直接计算出“标准答案”。策略网络 $\pi_T$ 输出的是一个**物理残差 (Physics-based Residual)**。

理想的残差动作 $a_t^*$ 是通过严格的物理公式推导出来的：

- **推力补偿**：为了不掉高度，推力必须额外抵消掉载荷的重力。
    
    $$a_{thrust}^* \propto |g| \cdot m_{load}$$
- **力矩补偿**：抵消质心偏移力矩和陀螺效应差值。
    
    $$\tau_{res}^* = -\tau_{grav} + \tau_{gyro}$$

**核心逻辑**：因为教师的动作是直接由牛顿-欧拉方程算出来的，所以**教师的输出天然符合物理准则**。

## 3. 第二阶段：适应性学生策略 (The Adaptive Student)

在真实世界中，机器人没有“上帝视角”。学生网络（Student Policy）的任务是：**仅凭历史观测数据，推断出当前的物理环境** $z_t$**，并模仿教师的动作。**

### 3.1 学生“看”到了什么？（历史观测序列）

学生只能看到机载传感器的数据。我们将历史窗口设定为 **100步 (**$K=100$**)**：

$$H_t = \{o_{t-99}, ..., o_{t-1}, o_t\}$$

选择 100 步的长窗口是为了确保能捕捉到那些**变化缓慢**的物理特性（如持续的重心偏移）。

### 3.2 为什么学生网络要用 CNN？

第一阶段我们用了 MLP，但第二阶段我们改用 **一维卷积神经网络 (1D-CNN)**。这主要基于：

1. **长时序特征提取**：CNN 擅长从长序列中提取频域特征。
    
2. **Sim-to-Real 部署优势**：在嵌入式 NPU 上，CNN 比 RNN 具有更高的并行效率和量化鲁棒性。
    

### 3.3 学生的学习目标：模仿 vs 物理约束

学生网络的训练是一个**监督学习**过程。它的基础目标是让自己生成的隐变量 $\hat{z}_t$ 尽可能接近教师的真实编码 $z_t$。

$$\mathcal{L}_{imit} = || \mathcal{E}_{MLP}(e_t) - \phi_{CNN}(H_t) ||^2$$

**深度辨析：为什么模仿了基于物理的老师，还需要 PINN？**

这是一个常见的误区。虽然**老师（Target）**是符合物理的，但**学生（Network）**本质上是一个数据拟合器。

- **模仿学习 (IL) 是“知其然”**：
    
    - 学生网络学会了“当姿态倾斜 $5^{\circ}$ 时，输出 $2N$ 力矩”，因为它看到老师是这么做的。
        
    - 但学生网络并不知道**为什么**要这么做。它只是记住了“输入-输出”的对应关系（Curve Fitting）。
        
    - **风险**：当 Sim-to-Real 遇到未知的噪声数据（数据分布外），学生网络可能会给出一个违反常识的预测，因为它没有内在的逻辑约束。
        
- **物理约束 (PINN) 是“知其所以然”**：
    
    - PINN 引入了 $F=ma$ 等硬性约束。它告诉学生网络：“不管你怎么模仿，你的输出和状态的导数关系必须满足牛顿定律”。
        
    - **收益**：即使在未知工况下，物理定律限制了学生网络的搜索空间，防止其输出离谱的控制量。
        

## 4. 训练难点分析

目前学生网络的训练效果尚未达到预期。从理论上分析，可能存在以下瓶颈：

1. **可观测性不足 (Observability Issue)**：可能存在多种不同的环境参数组合导致极其相似的历史轨迹。这种“多义性”让 CNN 难以唯一确定正确的 $z_t$。
    
2. **特征提取不匹配**：CNN 的感受野可能未能有效覆盖长期的积分效应。
    

## 5. Sim-to-Real 部署策略：先辨识，后随机

为了让算法在真机上也能跑通，我们并没有采取盲目的“大撒网”式随机化，而是采用了 **“离线 PINN 辨识 + 针对性域随机化”** 的策略。

### 5.1 离线物理参数辨识 (Offline System ID via PINNs)

在部署前，我们先采集约 5 分钟的真实飞行数据。利用 **参数化 PINN**，我们可以从这些数据中反求出机器人的真实物理参数（如真实质量 $m$、阻力系数 $D$）。

$$L_{ID} = L_{data} + \lambda L_{physics}$$

这就相当于给真实机器人做了一次全面的“体检”，获得了一组高精度的标称参数 $\hat{\boldsymbol{\theta}}_{nom}$。

### 5.2 针对性域随机化

回到仿真环境，我们围绕这个标称参数 $\hat{\boldsymbol{\theta}}_{nom}$ 构建高斯分布进行采样。这确保了策略是为这台特定机器人定制的，同时保留了鲁棒性。

## 6. 学生网络优化进阶：如何让学生“懂”物理？

针对您提出的“如何让学生网络优化得更好”以及“保证 8 位参数符合物理准则”的问题，我们提出以下进阶方案：

### 6.1 保证 8 位编码的物理意义：辅助任务 (Auxiliary Task)

**问题**：单纯的模仿学习无法保证 $z_t$ 的 8 个维度分别代表什么。它们可能是一堆混合的抽象特征。

**解法**：我们强迫学生网络“翻译”出物理参数。在 CNN 末端增加一个**辅助分支**，直接预测显式的物理量（如质量 $m$、质心偏移 $r$）。

$$L_{total} = \underbrace{|| z_t - \hat{z}_t ||^2}_{\text{隐变量模仿}} + \beta \underbrace{|| \mathbf{p}_{phy} - \hat{\mathbf{p}}_{phy} ||^2}_{\text{显式物理回归}}$$

**作用**：

- 这也回答了“如何保证 8 位参数符合物理准则”：如果这个辅助损失很低，说明 $z_t$ 确实包含了真实的物理信息，而不仅仅是过拟合的数据特征。
    
- 这引导 CNN 的卷积核去提取那些**真正具有物理含义**的频率特征，而非环境噪声。
    

### 6.2 提升泛化能力：物理一致性正则化 (PINN-style Regularization)

**问题**：如何防止学生网络在 Sim-to-Real 时“乱猜”？

**解法**：利用 PINN 思想，将动力学方程作为正则项加入训练。我们检查：**如果施加学生预测的动作，机器人的状态转移是否符合物理方程？**

$$L_{consistent} = || \text{Dynamics}(s_t, \pi_{base}(s_t) + \hat{a}_t; \hat{\theta}_{nom}) - s_{t+1} ||^2$$

**作用**： 这相当于给学生网络加了一道“验算”工序。即使在训练数据没覆盖到的地方，只要物理定律适用，这个 Loss 就会惩罚那些违背物理的预测，从而显著提升 Student 网络的**外推能力 (Extrapolation)**。

## 7. 实验验证与性能评估计划

为了全面验证所提方法的有效性，除“特权信息消融实验”外，我们设计了以下四个维度的对比实验：

### 7.1 架构有效性消融实验 (Architecture Ablation)

- **实验目的**：验证 CNN 相比传统时序网络在处理长历史窗口时的优势。
    
- **对比设置**：
    
    - **Student-CNN (Ours)**：基于 1D-CNN 处理 100 步历史。
        
    - **Student-LSTM**：基于 LSTM 处理 100 步历史。
        
    - **Student-MLP**：直接将 100 步历史 Flatten 后输入 MLP。
        
- **预期结论**：CNN 应在训练收敛速度（并行计算优势）和抗噪声干扰能力上优于 LSTM，且在 NPU 部署时的推理延迟显著降低。
    

### 7.2 物理感知组件有效性 (Physics-Awareness Ablation)

- **实验目的**：验证辅助任务和 PINN 正则化是否提升了隐变量的物理可解释性。
    
- **对比设置**：
    
    - **Baseline**：仅使用 $L_{imit}$ 进行模仿学习。
        
    - **Ours-Aux**：增加辅助回归任务 ($L_{phy\_aux}$)。
        
    - **Ours-PINN**：增加物理一致性正则化 ($L_{consistent}$)。
        
- **关键指标**：
    
    - **Latent Space Visualization**：使用 t-SNE 可视化 $z_t$ 分布，观察是否按物理参数（如质量大小）呈现线性聚类。
        
    - **Correlation Analysis**：计算预测的 $z_t$ 与真实物理参数的相关系数。
        

### 7.3 域外泛化压力测试 (OOD Stress Test)

- **实验目的**：模拟 Sim-to-Real 场景中可能出现的“未见工况”，验证算法鲁棒性。
    
- **设置**：
    
    - **训练集**：载荷质量 $m \in [0.0, 2.0]kg$。
        
    - **测试集 (OOD)**：载荷质量 $m \in [2.0, 3.5]kg$ （超出训练范围）。
        
- **预期结论**：
    
    - Baseline 方法在 OOD 区域可能会产生剧烈震荡或坠机。
        
    - 引入 PINN 约束的方法由于受物理方程限制，即使在未知区域也能保持基本的控制稳定性（Soft Failure），而非完全失效。
        

### 7.4 模仿机制消融：特征蒸馏 vs 行为克隆 (Distillation Mechanism)

- **实验目的**：这是回答“起作用的究竟是动作模仿，还是真正学到了物理信息？”的关键实验。我们将 **“隐变量对齐（Feature Matching）”** 和 **“直接动作克隆（Behavior Cloning）”** 解耦对比。
    
- **对比设置**：
    
    - **Method A (Action Cloning)**：学生网络忽略隐变量 $z_t$，直接回归教师的输出动作。
        
        - 架构：$H_t \to CNN \to MLP \to \hat{a}_t$
            
        - 损失函数：$L = || a_t^* - \hat{a}_t ||^2$
            
    - **Method B (Feature Distillation / Ours)**：学生网络仅学习逼近教师的隐变量 $z_t$，然后复用教师训练好的决策头（Policy Head）。
        
        - 架构：$H_t \to CNN \to \hat{z}_t \to \pi_{Teacher\_Head} \to \hat{a}_t$
            
        - 损失函数：$L = || z_t - \hat{z}_t ||^2$
            
- **预期结论与深度分析**：
    
    - **在分布内（训练集）**：Method A 和 Method B 的动作误差可能非常接近，因为简单的拟合足以应付已知数据。
        
    - **在分布外（OOD/Sim-to-Real）**：**Method B (Feature Distillation) 应显著优于 Method A**。
        
        - **原因**：动作 $a_t$ 是高频、非线性的，直接拟合动作容易过拟合于特定的轨迹模式。而特权信息 $z_t$（如质量、惯量）是低频、物理上连续的。Method B 强迫学生去理解“物理参数”，一旦学生在测试时猜对了 $z_t$（比如“现在很重”），教师的 Policy Head（拥有完美的物理逻辑）就能自动生成正确的应对动作。
            
        - **结论**：真正起作用的是**“学到了特权网络的信息（物理特征）”**，这才是泛化性的来源，而不仅仅是对残差动作的机械模仿。
            

## 8. 危机应对：如果特权网络失效怎么办？

在实际训练中，我们可能会遇到**“特权无效悖论”**：即在广泛的负载随机化下，盲网络（Blind Policy，不使用特权信息）的表现与教师网络几乎一样好。这意味着基础观测 $o_t$ 已经包含了足够的信息让 MLP 过拟合当前的随机化分布，导致特权网络 $z_t$ 沦为冗余。

针对这种情况，我们提出三层递进的激活策略：

### 8.1 方案一：加大难度 (Curriculum Hardening)

- **诊断**：当前的随机化分布对于 MLP 来说太“简单”了。例如，如果只随机化标量质量 $m$，网络很容易从 $a = F/m$ 中反推出来。
    
- **对策**：引入**高频动态随机化**和**极端不对称性**。
    
    - **时变参数**：让质量 $m(t)$ 在 episode 过程中动态变化（模拟燃料消耗或货物晃动），而非每个 episode 固定。
        
    - **不对称扰动**：引入极端的质心偏移 $r_{com}$ 或单侧电机效率衰减。这种复杂的动力学耦合很难被简单 MLP 从瞬时状态中“猜”出来，迫使网络必须依赖 $z_t$ 提供的全局物理上下文。
        

### 8.2 方案二：强制信息瓶颈 (Information Bottleneck)

- **诊断**：学生网络过于强大，凭借自身的算力“作弊”拟合了数据。
    
- **对策**：人为制造“信息缺失”，强迫学生依赖 $z_t$。
    
    - **Sensor Masking**：在训练时随机 Mask 掉部分关键观测（如角速度），此时如果没有 $z_t$ 的指导，盲网络将彻底失效。
        
    - **Latent Dropout**：在训练初期，以高概率 Drop 掉 $z_t$ 通道，强迫学生在没有老师时“受挫”，然后逐渐引入 $z_t$。如果 $z_t$ 真的有用，引入后的性能跃升将证明其价值。
        

### 8.3 方案三：因果干预与辅助损失 (Causal Intervention)

- **诊断**：网络收敛到了局部最优，认为 $z_t$ 是噪声。
    
- **对策**：通过 Loss 强行建立 $z_t$ 与 物理参数的因果联系。
    
    - **不学好不给分**：将 **6.1 节** 的辅助任务损失权重 $\beta$ 调大。即使动作预测得很准，只要 $z_t$ 不能准确回归出质量 $m$，总 Loss 就不下降。
        
    - **对比学习 (Contrastive Learning)**：构建正负样本对。同一物理环境下的不同轨迹，$z_t$ 应相近；不同物理环境下的相似轨迹，$z_t$ 应远离。这能拉大特征空间中物理参数的区分度。
        

**结论**：如果上述手段都用了，特权网络依然没用，那说明**当前任务的物理复杂度过低**，确实不需要特权信息。但在四足/旋翼等欠驱动系统的抗扰动任务中，只要扰动足够复杂，特权信息的价值通常是不可替代的。